coverage:
  precision: 0
  status:
    patch: off
    project:
      default: off
      managementserver:
        flags:
          - managementserver
          - pkg
      envoyauth:
        flags:
          - envoyauth
          - pkg
      controlplane:
        flags:
          - controlplane
          - pkg
      envoyals:
        flags:
          - envoyals
          - pkg

flags:
  managementserver:
    paths:
      - cmd/managementserver/
  envoyauth:
    paths:
      - cmd/frontend/
  controlplane:
    paths:
      - cmd/controlplane/
  envoyals:
    paths:
      - cmd/envoyals/
  pkg:
    paths:
      - pkg/
