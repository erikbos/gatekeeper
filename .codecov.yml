coverage:
  precision: 0
  status:
    patch: off
    project:
      default: off
      dbadmin:
        flags:
          - dbadmin
          - pkg
      envoyauth:
        flags:
          - envoyauth
          - pkg
      envoycp:
        flags:
          - emvoycp
          - pkg
      envoyals:
        flags:
          - emvoycp
          - pkg

flags:
  dbadmin:
    paths:
      - cmd/dbadmin/
  envoyauth:
    paths:
      - cmd/frontend/
  envoycp:
    paths:
      - cmd/envoycp/
  envoyals:
    paths:
      - cmd/envoyals/
  pkg:
    paths:
      - pkg/
