coverage:
  precision: 0
  status:
    patch: off
    project:
      default: off
      dbadmin:
        flags:
          - dbadmin
          - pkg
      envoyauth:
        flags:
          - envoyauth
          - pkg
      controlplane:
        flags:
          - controlplane
          - pkg
      envoyals:
        flags:
          - envoyals
          - pkg

flags:
  dbadmin:
    paths:
      - cmd/dbadmin/
  envoyauth:
    paths:
      - cmd/frontend/
  controlplane:
    paths:
      - cmd/controlplane/
  envoyals:
    paths:
      - cmd/envoyals/
  pkg:
    paths:
      - pkg/
